/**
 * Enum representing the different modes of the editor.
 */
export enum EditorMode {
    /**
     * Mode for selecting objects in the editor.
     */
    SELECT = "select",

    /**
     * Mode for moving objects in the editor.
     */
    MOVE = "move"
}
