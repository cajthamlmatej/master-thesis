import {EditorAction} from "@/editor/actions/EditorAction";

/**
 * Represents an abstract context-specific action in the editor.
 * This class is extended by actions that are executed in specific contexts.
 */
export abstract class ContextAction extends EditorAction {

}
